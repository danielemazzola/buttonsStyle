
/*
  SIMPLES
*/
export const ButtonCancel = ({contentCancel}) => {
  return (
    <button type={contentCancel.type} className={contentCancel.className}>{contentCancel.value}</button>
  )
}

export const ButtonConfirm = ({contentConfirm}) => {
    return (
      <button type={contentConfirm.type} className={contentConfirm.className}>{contentConfirm.value}</button>
    )
  }

  export const ButtonAdd = ({contentAdd}) => {
    return (
      <button type={contentAdd.type} className={contentAdd.className}>{contentAdd.value}</button>
    )
  }

  export const ButtonCancel1 = ({contentCancel1}) => {
    return (
      <button type={contentCancel1.type} className={contentCancel1.className}>{contentCancel1.value}</button>
    )
  }
  
  export const ButtonConfirm1 = ({contentConfirm1}) => {
      return (
        <button type={contentConfirm1.type} className={contentConfirm1.className}>{contentConfirm1.value}</button>
      )
    }
  
    export const ButtonAdd1 = ({contentAdd1}) => {
      return (
        <button type={contentAdd1.type} className={contentAdd1.className}>{contentAdd1.value}</button>
      )
    }

  export const ButtonCancel2 = ({contentCancel2}) => {
    return (
      <button type={contentCancel2.type} className={contentCancel2.className}>{contentCancel2.value}</button>
    )
  }
  
  export const ButtonConfirm2 = ({contentConfirm2}) => {
      return (
        <button type={contentConfirm2.type} className={contentConfirm2.className}>{contentConfirm2.value}</button>
      )
    }
  
    export const ButtonAdd2 = ({contentAdd2}) => {
      return (
        <button type={contentAdd2.type} className={contentAdd2.className}>{contentAdd2.value}</button>
      )
    }

  
/*
  SIMPLES-AVANZADOS
*/
export const ButtonCancelAdvance = ({contentCancelAdvance}) => {
  return (
    <button type={contentCancelAdvance.type} className={contentCancelAdvance.className}>{contentCancelAdvance.value}</button>
  )
}

export const ButtonConfirmAdvance = ({contentConfirmAdvance}) => {
    return (
      <button type={contentConfirmAdvance.type} className={contentConfirmAdvance.className}>{contentConfirmAdvance.value}</button>
    )
  }

  export const ButtonAddAdvance = ({contentAddAdvance}) => {
    return (
      <button type={contentAddAdvance.type} className={contentAddAdvance.className}>{contentAddAdvance.value}</button>
    )
  }

  /*
    SIMPLES-AVANZADOS-COLORES
  */
    export const ButtonCancelAdvanceColors = ({contentCancelAdvanceColors}) => {
      return (
        <button type={contentCancelAdvanceColors.type} className={contentCancelAdvanceColors.className}>{contentCancelAdvanceColors.value}</button>
      )
    }
    
    export const ButtonConfirmAdvanceColors = ({contentConfirmAdvanceColors}) => {
        return (
          <button type={contentConfirmAdvanceColors.type} className={contentConfirmAdvanceColors.className}>{contentConfirmAdvanceColors.value}</button>
        )
      }
    
      export const ButtonAddAdvanceColors = ({contentAddAdvanceColors}) => {
        return (
          <button type={contentAddAdvanceColors.type} className={contentAddAdvanceColors.className}>{contentAddAdvanceColors.value}</button>
        )
      }